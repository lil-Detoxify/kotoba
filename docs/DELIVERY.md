# Web MVP 交付记录

日期：2026-09-15。

## 结果

完成文档、npm workspaces、共享模型、纯 TS 学习核心、FSRS、CSV/JSON 导入预览、IndexedDB 事务、Vue Web 全部 MVP 页面。预置 4 课 48 词，示例在 examples/。

## 验证

- TypeScript strict + Vite 生产构建通过，主 JS 约 185 KB（gzip 67 KB）。
- Vitest：24 个测试通过。覆盖任意课次与范围、new/review/difficult 队列、忽略恢复、FSRS 三档评价、精确到期、统计与连续天数、CSV 引号/BOM/换行、JSON 校验、重复和非法音频、IndexedDB 日期保留、失败回滚和并发事务、级联删除。
- Playwright + Microsoft Edge：桌面完整验收与 390px 移动验收，两条用例通过。包含 CSV/JSON 导入、选择第2/4课、23次评价与1次忽略、生词、恢复、统计、刷新、推进浏览器时钟后真实到期复习、编辑和删除词书。
- 开发模式浏览器加载成功，无 pageerror。
- 桌面与移动截图已人工检查，位于 screenshots/；修正装饰日文换行与路由滚动复位。
- npm install 审计：0 vulnerabilities（升级测试依赖至已修复版本）。

## 限制

无云同步、完整备份、音频素材库、小程序/Windows/iOS 实现。未做微信/iOS 真机兼容及大词库性能压测。V1 全量快照存储需在长期大量日志前改为按实体存储。具体运行方式、示例说明与下一阶段问题见 README。

## V0.2 追加交付：详细释义与客观复习

- 已实现：词书多义项、217538条带来源的词典扩展、日文解释与学习例句；经典/混合/假名选中文/汉字选假名/听音选中文；50段真实生成的日语合成音频和慢速播放。
- 评分：首答锁定，错/不认识 Again，对 Good，猜对 Hard；继续时状态与含 quiz 快照的日志同事务保存。
- 37 项核心测试通过；5 条 Edge 浏览器验收通过（原流程2条、新增词典与题型3条）。类型检查和生产构建通过。桌面与390px移动截图已检查，新题型在手机使用专注布局。
- 词典数据约212 MiB，按需加载单分片，最大约491 KiB；首屏 JS gzip约76 KiB。
- 来源限制：JMdict/Tomoshi开放数据含机器辅助译释，非出版社审定；权威辞典通过コトバンク外链查阅，未接入商业辞典API。
- 仍需微信/iOS真机语音与文件兼容验证，完整词典不可直接装入小程序代码包。
- 设计与许可详见 DICTIONARY_AND_REVIEW.md；数据署名与修改声明位于 public/dictionary/KOTOBA-NOTICE.md。

## 本地服务恢复
5173/5174 均无监听，HTTP 返回 ECONNREFUSED，确认临时服务已停止。恢复独立后台5174进程并增加 npm start：固定origin、幂等检测、隐藏窗口与日志，保留原数据库。恢复后真实浏览器详细释义加载成功。此前词典提示未独立复现，不能断言所有既往失败都源于停服。
