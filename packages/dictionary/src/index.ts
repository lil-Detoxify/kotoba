export interface DictionaryEntry {id:string;terms:string[];readings:{text:string;restrictedTo:string[]}[];senses:{pos:string[];zh:string[];en:string[]}[];japanese:{source:string;texts:string[]}[]}
export interface DictionaryProvider {lookup(term:string,reading:string):Promise<DictionaryEntry[]>}
export function dictionaryBucket(text:string){let hash=0;for(const c of text.normalize('NFKC'))hash=(hash*31+c.codePointAt(0)!)>>>0;return hash%512}
const kana=(s:string)=>s.normalize('NFKC').replace(/[ァ-ヶ]/g,c=>String.fromCharCode(c.charCodeAt(0)-0x60));
export function matchEntries(entries:DictionaryEntry[],term:string,reading:string){return entries.filter(e=>(e.terms.includes(term)||e.readings.some(r=>r.text===term))&&e.readings.some(r=>kana(r.text)===kana(reading)&&(!r.restrictedTo.length||r.restrictedTo.includes(term)||!e.terms.length)))}
export function partOfSpeechLabel(pos:string[]){return pos.map(p=>{if(p==='prefix')return '接头词';if(p==='suffix')return '接尾词';if(p==='counter')return '量词';if(p==='numeric')return '数词';if(p==='interjection (kandoushi)')return '感叹词';if(p.includes('noun (common)'))return '名词';if(p.includes('pronoun'))return '代词';if(p.includes('adverb'))return '副词';if(p.includes('Godan'))return '五段动词';if(p.includes('Ichidan'))return '一段动词';if(p.includes('suru'))return 'サ变动词';if(p.includes('adjectival nouns'))return '形容动词';if(p.includes('adjective'))return '形容词';if(p.includes('expression'))return '惯用表达';return p}).join(' · ')}
